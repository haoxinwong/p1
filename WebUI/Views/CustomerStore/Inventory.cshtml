@model IEnumerable<WebUI.Models.InventoryVM>

@{
    ViewData["Title"] = "Inventory";
    Layout = "~/Views/Shared/_LayoutCustomer.cshtml";
    var i = 0;
}

<h1>Inventory</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @i
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            @*            <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>*@
        </tr>
            i += 1;
        }
    </tbody>

</table>
<br />
<p>Add Item to your shopping cart</p>



<form asp-action="ShoppingCart" method="post">
    <label>Item Id:</label>
    <br />
    <input id="ItemId" name="ItemId"></input>
    <br />
    <label>Item Quantity:</label>
    <br />
    <input id="ItemQuantity" name="ItemQuantity"></input>
    <br />
    <br />
    <button runat="server" type="submit" >Add to Shopping Cart</button>
</form>

<br />
<br />
<br />
<div>
    <form asp-action="ShoppingCart" method="get">
        <button type="submit">Check Shopping Cart</button>
    </form>
</div>
